package com.portfolio.management.domain.strategy.priority;

/**
 * Enum representing the different fields of a Stock that can be searched
 */
public enum SearchField {
    SYMBOL,
    NAME,
    EXCHANGE,
    TYPE,
    COUNTRY,
    CURRENCY
}
